bin/
# Built binaries at the repo root (anchored to avoid matching
# deploy/helm/comqtt-dashboard/ and similar paths).
/comqtt-dashboard
/comqtt-cluster-dashboard
*.log
data/

# Local working scratchpads (not user docs)
TODO.*.md
